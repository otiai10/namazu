source:
  type: p2pquake
  endpoint: wss://api-realtime-sandbox.p2pquake.net/v2/ws
  # Production: wss://api.p2pquake.net/v2/ws

subscriptions:
  - name: my-webhook
    delivery:
      type: webhook
      url: https://example.com/earthquake
      secret: your-secret-key-here
    # filter:                    # Optional (Phase 4)
    #   min_scale: 40            # Minimum seismic intensity
    #   prefectures:             # Target prefectures
    #     - 東京都
    #     - 神奈川県

# Optional: Enable Firestore storage (Phase 2+)
# When enabled, subscriptions can be managed dynamically via API
# and events are persisted for historical queries
# store:
#   type: firestore
#   project_id: your-gcp-project
#   database: "(default)"             # Optional: Firestore database name
#   credentials: service-account.json # Optional: Path to service account JSON

# Optional: Enable REST API server (Phase 2+)
# Provides endpoints for subscription management and event queries
# api:
#   addr: ":8080"
